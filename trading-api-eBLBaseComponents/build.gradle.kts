/* This placeholder is needed to inform Gradle that this generates a jar file.  Transitive
   dependencies will not resolve within Gradle without this.
 */
plugins {
    `java-library`
    `maven-publish`
}